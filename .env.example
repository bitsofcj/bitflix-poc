# Next.js Client-side Environment Variables (NEXT_PUBLIC_ prefix for client-side access)
NEXT_PUBLIC_GRAPHQL_URL=/api/graphql
NEXT_PUBLIC_CACHE_TTL_SECONDS=60
NEXT_PUBLIC_ITEMS_PER_PAGE=12

# Server-side Only Environment Variables (API Keys - kept secret from client)
MOVIES_API_URL="<your-api-url>"
MOVIES_API_KEY="<your-api-key>"

# Request Timeout Configuration
FETCH_TIMEOUT_MS=10000
